<ui:FluentWindow x:Class="MelinaWindowTopmost.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:vm="clr-namespace:MelinaWindowTopmost.ViewModels"
                 mc:Ignorable="d"
                 Title="{Binding Path=Localization[AppName]}"
                 Icon="Assets/maki5.ico"
                 Width="608"
                 Height="680"
                 MinWidth="560"
                 MinHeight="460"
                 Background="{DynamicResource ApplicationBackgroundBrush}"
                 ExtendsContentIntoTitleBar="True"
                 FontFamily="Microsoft YaHei"
                 d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}">
    <ui:FluentWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <Style x:Key="IconButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Width" Value="38" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="6,0,0,0" />
        </Style>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar x:Name="RootTitleBar"
                     Grid.Row="0"
                     Height="44"
                     Title="{Binding Path=Localization[AppName]}"
                     ShowMinimize="True"
                     ShowMaximize="True"
                     ShowClose="True"
                     CanMaximize="True" />

        <Grid Grid.Row="1" Margin="16,6,16,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:TextBox Grid.Column="0"
                        Height="36"
                        VerticalContentAlignment="Center"
                        PlaceholderText="{Binding Path=Localization[SearchWindowTitle]}"
                        Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                        ToolTip="{Binding Path=Localization[SearchWindowTitle]}" />

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                <Button Style="{StaticResource IconButtonStyle}"
                        Command="{Binding RefreshCommand}"
                        ToolTip="{Binding Path=Localization[Refresh]}">
                    <ui:SymbolIcon Symbol="ArrowClockwise24" />
                </Button>
                <Button Style="{StaticResource IconButtonStyle}"
                        Click="SettingsButton_Click"
                        ToolTip="{Binding Path=Localization[Settings]}">
                    <ui:SymbolIcon Symbol="Settings24" />
                </Button>
                <Button Style="{StaticResource IconButtonStyle}"
                        Click="AboutButton_Click"
                        ToolTip="{Binding Path=Localization[About]}">
                    <ui:SymbolIcon Symbol="Info24" />
                </Button>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2"
                Margin="0"
                Background="{DynamicResource ControlFillColorDefaultBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ListBox x:Name="WindowList"
                         Grid.Row="0"
                         ItemsSource="{Binding FilteredWindows}"
                         SelectedItem="{Binding SelectedWindow}"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.CanContentScroll="True"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         MouseDoubleClick="WindowList_MouseDoubleClick"
                         PreviewMouseRightButtonDown="WindowList_PreviewMouseRightButtonDown">
                    <ListBox.ContextMenu>
                        <ContextMenu Opened="WindowContextMenu_Opened"
                                     FontFamily="Microsoft YaHei">
                            <MenuItem Header="{Binding PlacementTarget.DataContext.Localization[MenuSwitchTo], RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      FontWeight="Bold"
                                      Click="SwitchToMenuItem_Click" />
                            <Separator />
                            <MenuItem x:Name="TopMostMenuItem"
                                      Header="{Binding PlacementTarget.DataContext.Localization[MenuTopMost], RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      Click="TopMostMenuItem_Click" />
                            <MenuItem x:Name="CancelTopMostMenuItem"
                                      Header="{Binding PlacementTarget.DataContext.Localization[MenuCancelTopmost], RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      Click="CancelTopMostMenuItem_Click" />
                            <Separator />
                            <MenuItem Header="{Binding PlacementTarget.DataContext.Localization[MenuSetOpacity], RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                <MenuItem Header="100%" Tag="100" Click="OpacityMenuItem_Click" />
                                <MenuItem Header="90%" Tag="90" Click="OpacityMenuItem_Click" />
                                <MenuItem Header="75%" Tag="75" Click="OpacityMenuItem_Click" />
                                <MenuItem Header="50%" Tag="50" Click="OpacityMenuItem_Click" />
                                <MenuItem Header="25%" Tag="25" Click="OpacityMenuItem_Click" />
                                <MenuItem Header="{Binding PlacementTarget.DataContext.Localization[MenuOpacityHidden], RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          Tag="0"
                                          Click="OpacityMenuItem_Click" />
                                <Separator />
                                <MenuItem Header="{Binding PlacementTarget.DataContext.Localization[MenuCustom], RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          Click="CustomOpacityMenuItem_Click" />
                            </MenuItem>
                            <Separator />
                            <MenuItem Header="{Binding PlacementTarget.DataContext.Localization[MenuShowInfo], RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      Click="InformationMenuItem_Click" />
                            <MenuItem Header="{Binding PlacementTarget.DataContext.Localization[MenuOpenLocation], RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      Click="OpenLocationMenuItem_Click" />
                            <MenuItem Header="{Binding PlacementTarget.DataContext.Localization[MenuOpenUwpInstallPath], RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      Visibility="{Binding PlacementTarget.DataContext.SelectedWindow.HasUwpInstallPath, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource BoolToVisibilityConverter}}"
                                      Click="OpenUwpInstallPathMenuItem_Click" />
                        </ContextMenu>
                    </ListBox.ContextMenu>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Grid>
                                            <Border x:Name="BaseBackground"
                                                    Background="Transparent"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}" />
                                            <Border x:Name="HoverOverlay"
                                                    Margin="1"
                                                    CornerRadius="4"
                                                    Background="#32FF6699"
                                                    Opacity="0" />
                                            <Border x:Name="SelectedBackground"
                                                    Margin="1"
                                                    CornerRadius="4"
                                                    Background="#69FF6699"
                                                    Opacity="0" />
                                            <Border x:Name="SelectedHoverOverlay"
                                                    Margin="1"
                                                    CornerRadius="4"
                                                    Background="#32FF6699"
                                                    Opacity="0" />
                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsTopMost}" Value="True">
                                                <Setter TargetName="BaseBackground" Property="Background" Value="#32FF6699" />
                                            </DataTrigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="SelectedBackground" Property="Opacity" Value="1" />
                                            </Trigger>
                                            <MultiTrigger>
                                                <MultiTrigger.Conditions>
                                                    <Condition Property="IsMouseOver" Value="True" />
                                                    <Condition Property="IsSelected" Value="False" />
                                                </MultiTrigger.Conditions>
                                                <Setter TargetName="HoverOverlay" Property="Opacity" Value="1" />
                                            </MultiTrigger>
                                            <MultiTrigger>
                                                <MultiTrigger.Conditions>
                                                    <Condition Property="IsMouseOver" Value="True" />
                                                    <Condition Property="IsSelected" Value="True" />
                                                </MultiTrigger.Conditions>
                                                <Setter TargetName="SelectedHoverOverlay" Property="Opacity" Value="1" />
                                            </MultiTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:WindowItemViewModel}">
                            <Border MinHeight="46"
                                    Margin="0">
                            <Grid Margin="10,3,4,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="40" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0"
                                        Width="34"
                                        Height="34"
                                        Background="{DynamicResource ControlFillColorSecondaryBrush}"
                                        VerticalAlignment="Center">
                                    <Image Source="{Binding Icon}" Width="26" Height="26" Stretch="Uniform" />
                                </Border>

                                <StackPanel Grid.Column="1" Margin="6,0,6,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding Subtitle}"
                                               Margin="0,1,0,0"
                                               FontSize="12"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                               TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

    </Grid>
</ui:FluentWindow>
